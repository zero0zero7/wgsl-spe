# Default config: run against all backend defaults (dawn:vk and wgpu:vk).
# CONFIGS is a space-separated list of backend IDs (from `wgslsmith harness
# list`), e.g. CONFIGS="dawn:vk:49374 wgpu:vk:49374". Empty = platform defaults.
CONFIGS=""
TIMEOUT=120
GEN_FLAGS=""
